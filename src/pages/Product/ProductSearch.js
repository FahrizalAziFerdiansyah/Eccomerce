import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Container, Search} from '../../components/atoms';

const ProductSearch = () => {
  return (
    <Container>
      <Search />
    </Container>
  );
};

export default ProductSearch;

const styles = StyleSheet.create({});
